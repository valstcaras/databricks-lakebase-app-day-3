command:
  - "python"
  - "weather_mcp_server.py"

resources:
  - name: requirements
    source:
      path: ./requirements.txt

env:
  # No API keys needed - Open-Meteo is completely free!
  # Optional: Add custom environment variables here if needed in the future
  - name: LOG_LEVEL
    value: "INFO"
